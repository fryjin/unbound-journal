# P0.7 Patch Upload

Target repository: `fryjin/unbound-journal`
Target branch: `main`

Upload the contents of this ZIP directly into the repository root.
Allow same-path files to replace the existing P0.6 versions and add the new files.

This patch contains only P0.7 changed/new source and docs. It does not duplicate the 40-paper starter asset pack.

Key additions:

- `packages/editor-core/src/history.ts`
- `packages/paper-engine/src/commands.ts`
- `docs/P0.7_HISTORY_SPEC.md`
- `docs/P0.7_RELEASE_NOTES.md`

Cloudflare deployment is intentionally not part of P0.7.
